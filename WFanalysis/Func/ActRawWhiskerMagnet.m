classdef ActRawWhiskerMagnet < ActRaw

    %% Methods
    methods
        %% Constructor
        function obj = ActRawWhiskerMagnet(param, wfTable, bpodTable)
            obj = obj@ActRaw(param, wfTable, bpodTable); % call the super class
        end

    end

end
